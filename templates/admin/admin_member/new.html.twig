{% extends 'base.html.twig' %}

{% block title %}Nouveaux Membres{% endblock %}

{% block body %}
    <h1 class="ms-2 text-center">Ajouter un nouveau membre </h1>
    <div class="row justify-content-center">
		<div class="w-50">
			{{ include('admin/admin_member/_form.html.twig') }}
			<div class="d-flex align-items-center justify-content-start">
				<a class=" btn btn-primary btn-lg m-2" href="{{ path('admin_member_index') }}">Retour à la liste</a>
			</div>
		</div>
	</div>
{% endblock %}
