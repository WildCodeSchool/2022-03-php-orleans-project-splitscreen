{% extends 'admin/baseAdmin.html.twig' %}

{% block title %}Actu{% endblock %}

{% block body %}
    <h1>Actu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ actu.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ actu.title }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ actu.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ actu.description }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ actu.date ? actu.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_actu_index') }}">back to list</a>

    <a href="{{ path('admin_actu_edit', {'id': actu.id}) }}">edit</a>

    {{ include('admin/admin_actu/_delete_form.html.twig') }}
{% endblock %}
