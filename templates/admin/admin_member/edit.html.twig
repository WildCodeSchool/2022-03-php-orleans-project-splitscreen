{% extends 'base.html.twig' %}

{% block title %}Modifier un membre
{% endblock %}

{% block body %}
	<div class="mt-5 ms-5 mb-5 ">
		<a href="{{ path('admin_member_index') }}">
			<i class="bi bi-arrow-left fs-1"></i>
		</a>
	</div>
	<h1 class="ms-2 text-center">Modifier le membre</h1>
	<div class="row justify-content-center">
		<div class="w-50">
			{{ include('admin/admin_member/_form.html.twig', {'button_label': 'Modifier'}) }}
		</div>
	</div>
{% endblock %}
