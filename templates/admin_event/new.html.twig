{% extends 'base.html.twig' %}

{% block title %}New Event
{% endblock %}

{% block body %}
	<h1 class="ms-2 d-flex justify-content-center">Create new Event</h1>
	<div class="row justify-content-center">
		<div class="w-50">
			{{ include('admin_event/_form.html.twig') }}
			<div class="d-flex align-items-center justify-content-start">
				<a type="button" class=" btn btn-primary btn-lg m-2" href="{{ path('admin_event_index') }}">back to list</a>
			</div>
		</div>
	</div>
{% endblock %}
