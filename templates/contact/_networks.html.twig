<div class="row">
	<div class="col-lg-6 align-self-center">
		<table class="table text-center align-middle">
			<tbody>
				<tr>
					{% for network in networks %}
					<tr>
						<td class="pb-3 text-primary d-flex flex-column">
							<h3>
								<a href="{{ network.link }}" class="link-primary text-decoration-none" target="_blank">{{ network.title }}
								</a>
							</h3>
						</td>
						
						<td <i class=" bi bi-{{network.title}} fs-3"></i> </td>
						</tr>
					{% endfor %}
				</tr>
			</tbody>
		</table>
	</div>
	<div class="col-lg-6">
		<img src="{{ asset('build/images/rs_contact.jpg') }}" alt="réseaux sociaux" class="w-100">
	</div>
</div></div></div>
