{% extends 'admin/baseAdmin.html.twig' %}

{% block javascripts %}
	{{ encore_entry_script_tags('participants') }}
{% endblock %}

{% block title %}{{parent()}}
	| Modifier l'événement
{% endblock %}

{% block body %}
	<h1 class="pt-4 text-center">Modifier l'événement</h1>
	<a href="{{ path('admin_event_index') }}" class="ms-5 btn btn-primary">
		<i class="bi bi-arrow-left"></i>
		Retour à la liste</a>
		<div class="w-50 mx-auto my-4 text-center">
			{{ include('admin/admin_event/_form.html.twig', {'button_label': 'Modifier'}) }}
		</div>
{% endblock %}
