<form method="post" action="{{ path('admin_network_delete', {'id': network.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ network.id) }}">
    <button class="btn">Delete</button>
</form>
