<div class="ms-1 border border-3 border-primary p-3">{{ form_start(form) }}
	{{ form_widget(form) }}
	<div class="text-center">
	<button class="btn btn-secondary">{{ button_label|default('Valider') }}<i class="ms-1 bi bi-check-lg"></i></button>
	</div>
	{{ form_end(form) }}
</div>